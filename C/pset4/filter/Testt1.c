#include <cs50.h>
#include <stdio.h>
#include <string.h>
#include <math.h>

int main(void)
{
    float gg = 83;
    int hg = round(gg / 3);
    int average = round(hg);
    int vitto = pow(2, 2) * -2;
    printf("hg: %i \n", hg);
    printf("average: %i \n", average);
    printf("vitto: %i \n", vitto);
}